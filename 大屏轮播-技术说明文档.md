# iOS Banner Vue 项目技术说明文档

> 版本：1.0.0  
> 最后更新：2025年12月

---

## 一、项目概述

### 1.1 项目简介

本项目是一个基于 **Vue 3 + TypeScript + Vite** 技术栈开发的全屏轮播展示系统，主要用于大屏展示场景。项目名称为 `iosbanner-vue`，实现了电影级沉浸式切换效果的图片轮播功能。

### 1.2 核心功能

| 功能模块 | 描述 |
|---------|------|
| 全屏轮播展示 | 电影级 Ken Burns 缩放效果 + Fade 淡入淡出过渡 |
| 数据接口对接 | 从后端 API 获取轮播图片数据 |
| 增量数据同步 | 支持轮询机制，实时同步新增/删除的图片数据 |
| 品牌展示 | 左上角品牌标题 + 弧光动画效果 |
| 二维码展示 | 右下角小程序二维码入口 |
| 响应式布局 | 适配不同屏幕尺寸 |

### 1.3 技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 前端框架 | Vue.js | ^3.5.24 |
| 构建工具 | Vite | ^7.2.4 |
| 编程语言 | TypeScript | ~5.9.3 |
| HTTP 客户端 | Axios | ^1.13.2 |
| 轮播组件 | Swiper | ^12.0.3 |
| 类型检查 | vue-tsc | ^3.1.4 |

---

## 二、项目结构

```
iosbanner-vue/
├── public/                    # 静态资源目录
├── src/
│   ├── api/                   # API 接口层
│   │   └── carousel.ts        # 轮播图数据接口
│   ├── assets/                # 静态资源
│   │   └── images/            # 本地图片资源
│   ├── components/            # 组件目录
│   │   ├── HeroSlider/        # 核心轮播组件
│   │   │   └── index.vue      # 主轮播组件（1000+ 行）
│   │   ├── WaveCarousel/      # 波浪轮播组件（备用）
│   │   └── HelloWorld.vue     # 示例组件
│   ├── composables/           # 组合式函数
│   ├── config/                # 配置文件
│   │   └── index.ts           # 应用配置
│   ├── data/                  # 本地数据
│   ├── types/                 # TypeScript 类型定义
│   │   └── carousel.ts        # 轮播相关类型
│   ├── App.vue                # 根组件
│   ├── main.ts                # 应用入口
│   ├── style.css              # 全局样式
│   └── vite-env.d.ts          # Vite 环境类型声明
├── .env.development           # 开发环境配置
├── .env.production            # 生产环境配置
├── index.html                 # HTML 入口
├── package.json               # 项目依赖配置
├── tsconfig.json              # TypeScript 配置
├── tsconfig.app.json          # 应用 TS 配置
├── tsconfig.node.json         # Node 环境 TS 配置
└── vite.config.ts             # Vite 构建配置
```

---

## 三、环境配置

### 3.1 环境变量说明

#### 开发环境 (`.env.development`)

```bash
# 开发环境配置
VITE_APP_ENV=development

# API 基础路径（用于代理转发）
VITE_APP_BASE_API=/dev-api

# API 服务器地址
VITE_APP_API_URL=https://k.rdzh8.com

# OSS 图片域名
VITE_APP_OSS_URL=https://devztj.oss-cn-beijing.aliyuncs.com
```

#### 生产环境 (`.env.production`)

```bash
# 生产环境配置
VITE_APP_ENV=production

# API 基础路径
VITE_APP_BASE_API=/dev-api

# API 服务器地址
# VITE_APP_API_URL=https://o.rdzh8.com  # 测试环境
VITE_APP_API_URL=https://k.rdzh8.com    # 正式环境

# OSS 图片域名
VITE_APP_OSS_URL=https://devztj.oss-cn-beijing.aliyuncs.com
```

### 3.2 关键配置项说明

| 变量名 | 说明 | 注意事项 |
|--------|------|----------|
| `VITE_APP_BASE_API` | API 请求前缀 | 用于区分不同环境的 API 路由 |
| `VITE_APP_API_URL` | 后端服务地址 | 切换环境时需修改此配置 |
| `VITE_APP_OSS_URL` | 阿里云 OSS 域名 | 图片资源存储地址 |

---

## 四、核心模块详解

### 4.1 轮播组件 (`HeroSlider`)

**文件路径：** `src/components/HeroSlider/index.vue`

#### 功能特性

- ✅ **Ken Burns 效果**：图片持续缩放动画，增强视觉沉浸感
- ✅ **Fade 过渡**：平滑的淡入淡出切换效果
- ✅ **增量数据同步**：支持实时获取新增/删除的轮播图
- ✅ **轮询机制**：默认每 30 秒自动刷新数据
- ✅ **响应式设计**：适配 PC、平板、手机等设备

#### 组件 Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `data` | `SlideItem[]` | `[]` | 初始轮播数据 |
| `showIndicators` | `boolean` | `false` | 是否显示进度指示器 |
| `pollInterval` | `number` | `30000` | 轮询间隔（毫秒） |

#### 暴露的方法

```typescript
defineExpose({
  slides,         // 当前轮播数据
  activeIndex,    // 当前活动索引
  loadSlides,     // 加载数据
  refreshSlides,  // 增量刷新数据
  startPolling,   // 启动轮询
  stopPolling,    // 停止轮询
});
```

### 4.2 API 接口层 (`carousel.ts`)

**文件路径：** `src/api/carousel.ts`

#### 接口函数

| 函数名 | 说明 | 返回值 |
|--------|------|--------|
| `fetchCarouselData()` | 获取全量轮播数据 | `Promise<SlideItem[]>` |
| `syncCarouselItems(existingIds)` | 增量同步数据 | `Promise<CarouselSyncResult>` |

#### API 端点

```
GET /dev-api/system/file/screen
```

#### 响应数据结构

```typescript
interface CarouselApiResponse {
  code: string;      // "0" 表示成功
  msg: string;       // 响应消息
  success: boolean;  // 请求是否成功
  data: FileItem[];  // 文件列表
}
```

### 4.3 类型定义 (`carousel.ts`)

**文件路径：** `src/types/carousel.ts`

```typescript
// 前端使用的轮播项
interface SlideItem {
  id: string;       // 唯一标识
  image: string;    // 图片地址
  title: string;    // 主标题
  subtitle: string; // 副标题
}

// API 返回的文件项
interface FileItem {
  id: string;
  name: string;
  path: string;           // OSS 路径
  author: string | null;  // 作者
  jobTitle: string | null; // 职位
  // ... 其他字段
}
```

---

## 五、构建与部署

### 5.1 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

开发服务器默认监听 `0.0.0.0`，可通过局域网访问。

### 5.2 生产构建

```bash
# 生产环境构建
npm run build

# 开发环境构建（带 sourcemap）
npm run build:dev
```

#### 构建输出

- **输出目录：** `dist/`
- **资源目录：** `dist/assets/`
- **基础路径：** `/dev-daping`（生产环境）

### 5.3 Vite 构建配置要点

```typescript
// vite.config.ts 关键配置
{
  base: mode === "production" ? "/dev-daping" : "/",
  build: {
    outDir: "dist",
    chunkSizeWarningLimit: 2000,
    rollupOptions: {
      output: {
        manualChunks(id) {
          if (id.includes("node_modules")) {
            return "vendor";  // 第三方库单独打包
          }
        },
      },
    },
    assetsInlineLimit: 0,  // 禁用资源内联
  }
}
```

### 5.4 部署配置

#### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location /dev-daping {
        alias /path/to/dist;
        try_files $uri $uri/ /dev-daping/index.html;
    }
    
    # API 代理（如需服务端代理）
    location /dev-api {
        proxy_pass https://k.rdzh8.com;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 六、注意事项

### 6.1 ⚠️ 环境切换

1. **切换服务器地址**时，需修改 `.env.production` 中的 `VITE_APP_API_URL`
2. 测试环境地址：`https://o.rdzh8.com`
3. 正式环境地址：`https://k.rdzh8.com`

### 6.2 ⚠️ 图片资源

1. 所有图片资源均存储在**阿里云 OSS**
2. OSS 地址：`https://devztj.oss-cn-beijing.aliyuncs.com`
3. 图片路径中的中文字符已做 URL 编码处理

### 6.3 ⚠️ API 跨域

1. 开发环境通过 Vite 代理解决跨域问题
2. 生产环境需确保后端已配置 CORS 或使用 Nginx 反向代理

### 6.4 ⚠️ 轮播组件

1. 轮播切换速度：**1500ms**
2. 自动播放间隔：**5000ms**（5秒）
3. 数据轮询间隔：**30000ms**（30秒）
4. Ken Burns 动画时长：**8秒**

### 6.5 ⚠️ 浏览器兼容性

- ✅ Chrome（推荐）
- ✅ Firefox
- ✅ Safari
- ✅ Edge
- ⚠️ IE 不支持

### 6.6 ⚠️ 本地二维码图片

- 二维码图片位于：`src/assets/images/qrUrl.png`
- 如需更换二维码，替换此文件并重新构建

---

## 七、常见问题排查

### Q1: 图片无法加载

**可能原因：**
1. OSS 配置的域名不正确
2. OSS 跨域配置问题
3. 图片路径包含特殊字符未正确编码

**解决方案：**
- 检查 `.env.*` 中的 `VITE_APP_OSS_URL` 配置
- 确认 OSS Bucket 已开启跨域访问
- 检查浏览器控制台的网络请求

### Q2: API 请求失败

**可能原因：**
1. 环境变量配置错误
2. 后端服务不可用
3. 跨域被拦截

**解决方案：**
- 检查 `VITE_APP_API_URL` 配置
- 使用 Postman 测试 API 可用性
- 检查 Nginx 代理配置

### Q3: 构建后页面空白

**可能原因：**
1. 基础路径配置错误
2. 资源路径问题

**解决方案：**
- 确认 `vite.config.ts` 中 `base` 配置与部署路径一致
- 检查是否正确部署到 `/dev-daping` 路径

### Q4: 轮播数据不更新

**可能原因：**
1. 轮询机制未启动
2. API 返回数据格式变更

**解决方案：**
- 检查控制台日志中的增量同步信息
- 确认 API 响应的 `code` 字段值为 `"0"`


---

## 九、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2025-12 | 初始版本，包含核心轮播功能 |

---

*文档结束*
